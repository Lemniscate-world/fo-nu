name: fo_nu
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.4.3
  share_plus: ^7.0.0
  path_provider: ^2.0.0
  tflite_flutter: ^0.9.0
  receive_sharing_intent: ^1.8.0  # Updated to a specific version

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true